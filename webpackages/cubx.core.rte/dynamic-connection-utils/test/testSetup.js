(function () {
  'use strict';
  if (!window.cubx) {
    window.cubx = {};
  }
  // set global variable window.cif
  if (!window.cubx.cif) {
    window.cubx.cif = {};
  }
  console.log('!!!test setup -> window.cubx.cif', window.cubx.cif);
})();
